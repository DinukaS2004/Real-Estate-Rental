export default function PropertyDetailPage({
                                               params,
                                           }: {
    params: { id: string };
}) {
    return <div>Property {params.id} — coming soon</div>;
}